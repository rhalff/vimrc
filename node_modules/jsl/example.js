var arr = [1, 2, 3]

arr.reduce(function(lhs, rhs) {
  return lhs + rhs
}, 0)

arr
  .pipe(function() {
    test
  })
  .pipe(function() {
    test2
  })

asdf({
    a: 2
  , b: 3
}, 2, 3, {
    c: 4
  , d: 5
})

asdf(
    0, 1, 2
)

example(function() {
  var a = 1
  // a comment on the last line of a callback function
})

// this is just a test
// no, seriously. it's a test!
// you should really just come to accept it.
function test() {
  1 + 1

  // comment
  if(!message.audience) {
    message.audience = {}
  }
}
